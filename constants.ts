/**
 * Global game constants (canvas, tile size).
 * Room-specific config lives in rooms/; items in items/.
 */

export const CANVAS_WIDTH = 800;
export const CANVAS_HEIGHT = 600;
export const TILE_SIZE = 40;
